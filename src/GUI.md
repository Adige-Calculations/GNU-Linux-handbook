# GUI
```console
sudo systemctl set-default multi-user
```
Disable GUI
```console
gnome-session-quit 
```
Install Gnome GUI
```console
sudo systemctl start gdm3 
```
Enable GUI
```console
sudo systemctl set-default graphical 
```